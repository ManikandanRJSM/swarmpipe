name=nectar-postgres-source
connector.class=io.debezium.connector.postgresql.PostgresConnector
tasks.max=1

database.hostname=localhost
database.port=5432
database.user=swarmpipe
database.password=S#8055%
database.dbname=nectar_db

topic.prefix=nectar_de

table.include.list=public.sites,public.buildings,public.assets,public.sensors,public.iot_telemetry,public.events

plugin.name=pgoutput
publication.name=nectar_publication
slot.name=nectar_slot

snapshot.mode=initial

key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter
key.converter.schemas.enable=false
value.converter.schemas.enable=false